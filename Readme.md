1.How to run Source TestWebAPI:

  - set connection db:
   "ConnectionStrings": {
    "MySqlConnection": "server=localhost;port=3306;user=root;database=testwebapi; password=testwebapi",
    "MySqlVersion": "8.0.34-mysql"
  }
  
  To build and run your sample:

Go to the sample folder and build to check for errors:

dotnet build
Run prohect:

dotnet run

2. how to run unit test:
 
 - Clear  solution unit test.
 - Build solution unit test.
 - Run test case.